# ReadPp Fonts

Target typography:

- Titles: Playfair Display.
- Content: Inter.

Add the font files here before enabling explicit `fonts:` entries in
`pubspec.yaml`. The theme already references the family names so screens can be
built against the final typography contract.
